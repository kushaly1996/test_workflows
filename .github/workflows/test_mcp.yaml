name: mcp-basic-test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6

      - name: Dummy Run
        run: |
          echo "MCP Test Started"
          date
          echo "Running dummy command"
          sleep 5
          echo "MCP Test Completed"
